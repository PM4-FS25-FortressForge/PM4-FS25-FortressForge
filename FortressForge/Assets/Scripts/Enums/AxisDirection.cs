namespace FortressForge.Enums
{
    /// <summary>
    /// Represents the direction of an axis.
    /// </summary>
    public enum AxisDirection
    {
        Negative = -1,
        Positive = 1
    }
}